# ==========================================
# Math Crossword App - Environment Configuration
# ==========================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
# Get your database URL from:
# - Neon: https://neon.tech (Recommended)
# - Supabase: https://supabase.com
# - Local PostgreSQL: postgresql://username:password@localhost:5432/database_name

# Development Database (used when NODE_ENV=development)
DATABASE_URL_DEV=postgresql://user:password@dev-host:5432/kidsmath?sslmode=require&channel_binding=require

# Production Database (used when NODE_ENV=production)
DATABASE_URL_PROD=postgresql://user:password@prod-host:5432/kidsmath?sslmode=require&channel_binding=require

# Backward compatibility: If DATABASE_URL is set, it will be used as fallback
# DATABASE_URL=postgresql://user:password@host:5432/kidsmath?sslmode=require

# Session Configuration
SESSION_SECRET=change-this-to-a-random-secure-string-in-production

# Optional: Enable debug logging
# DEBUG=true
